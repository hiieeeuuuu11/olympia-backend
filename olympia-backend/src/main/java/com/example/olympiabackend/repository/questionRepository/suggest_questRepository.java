package com.example.olympiabackend.repository.questionRepository;

import com.example.olympiabackend.model.questions.suggest_quest;
import org.springframework.data.jpa.repository.JpaRepository;


public interface suggest_questRepository extends JpaRepository<suggest_quest, Integer> {
}
